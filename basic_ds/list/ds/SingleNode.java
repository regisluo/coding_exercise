package basic_ds.list.ds;

/**
 * Single node type;
 * Normally, this class will be given in interview or the platform like leetcode.
 * But if not, you can define it by yourself.
 */
public class SingleNode {
    public int value;
    public SingleNode next;
    public SingleNode(int value) {
        this.value = value;
    }
}
